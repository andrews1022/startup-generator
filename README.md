# Startup Generator

Simple startup generator app built using React & TypeScript

I build this version of the app slightly differently to pass data down via props, to get more practice with props + TypeScript.
